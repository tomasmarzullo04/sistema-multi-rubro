# Copiar a packages/db/.env para desarrollo local.
# Coincide con los defaults de docker-compose.yml en la raíz del repo.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sistema_multi_rubro"
